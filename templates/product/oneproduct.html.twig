
{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block content %}
    <h1> {{ product.name }}</h1>
    <div class="row mt-5">
            <div class="col-md-4">

                    <img src="/uploads/{{ product.image }}" alt="Card image cap">
                        <p>{{ product.price/100 }} €</p>
                        <p>{{ product.title }}</p>
                        <p>{{ product.description }}</p>
                <a href="{{ path('add', {'id':product.id}) }}">Ajouter au panier</a>
            </div>
    </div>


{% endblock %}